/**
 * Shared constants used across snapshotTasks.js and ocrTasks.js.
 * Centralised here so changing one value keeps both files in sync.
 */

/** Width in pixels of the separator stripe between panels in the composite diff image. */
const COMPOSITE_SEP = 8;

module.exports = { COMPOSITE_SEP };
